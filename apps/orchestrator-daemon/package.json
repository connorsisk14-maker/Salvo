{
  "name": "@salvo/orchestrator-daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@salvo/contracts": "workspace:*",
    "@salvo/db": "workspace:*",
    "@salvo/evaluation": "workspace:*",
    "@salvo/llm": "workspace:*",
    "@salvo/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
